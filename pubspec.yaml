name: ppfo_math_app
description: PPFO v25.0 - نظام رياضي متكامل by الدكتور سعودي محمد
publish_to: 'none'
version: 25.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  math: ^0.1.1
  decimal: ^2.3.0
  big_numbers: ^2.1.0
  flutter_launcher_icons: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon.png"

flutter:
  uses-material-design: true
  assets:
    - assets/
